[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "task-cli"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.9"

[project.scripts]
task-cli = "task_cli.mainCLI:main"

[tool.setuptools]
packages = ["task_cli"]